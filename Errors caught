1. Пустой Json (null).
    сообщение в логе: 'Файл <JsonName>  - на самом деле не Json. Он пуст или не соотвутствует структуре Json.
                            Перехожу к следующему файлу'.
                            
2. Смысловая часть файла (объект 'data') в файле = null. 
    Сообщение в логе: 'Раздел "data" файла <JsonName> пуст, это значит, что смысловая часть данных отсуствует. Перехожу к следующему файлу'
                                
    
3. Несоответствие формата данных условию схемы. 
    Сообщение в логе: 'Неправильный тип данных у <данные>. Замените на данные типа <тип данных>'

4. Отсутствие обязательных свойств/объектов.
    Сообщение в логе: 'В файле json не хватает обязательной части: {}, добавьте ее.'.format(<недостающая часть>)
    
    
 
